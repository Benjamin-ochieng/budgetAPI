import mongoose from 'mongoose'
import { Budget } from '../budget.model'

describe('Budget model', () => {
  describe('Schema', () => {
      test('overallSpend', () => {
        const overallSpend = Budget
        console.log(overallSpend)
      })
  })
})
